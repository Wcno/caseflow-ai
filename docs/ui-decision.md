# Decisión de interfaz

Se exploraron tres variantes en la rama `prototype/intake-ui`. Se escogió la variante A: captura a la izquierda y expediente a la derecha.

La separación mantiene visible el progreso mientras el colaborador entiende que el resultado sigue siendo revisable. El foco del MVP es un flujo único, no un tablero de operación bancaria.
