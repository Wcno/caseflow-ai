# Domain docs

This repository uses a single domain context.

- Read `/CONTEXT.md` before exploring or changing domain behavior.
- Read relevant decisions under `/docs/adr/` before architectural changes.
- Use the glossary's preferred terms in code, tests, issues, and documentation.
- If a needed domain term is missing, add it through the domain-modeling
  workflow instead of inventing a synonym.

