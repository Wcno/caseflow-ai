import { z } from "zod";export const productSchema = z.enum([  "tarjeta_debito",  "cuenta_ahorro",  "transferencia",  "banca_digital"]);export const claimStageSchema = z.enum([  "queued",  "transcribing",  "retrieving",  "analyzing",  "validating",  "ready",  "failed"]);export const intakeInputSchema = z.discriminatedUnion("kind", [  z.object({ kind: z.literal("text"), text: z.string().trim().min(8).max(8_000) }),  z.object({    kind: z.literal("audio"),    filePath: z.string().min(1),    mimeType: z.string().min(1)  })]);export const procedureCitationSchema = z.object({  id: z.string().min(1),  title: z.string().min(1),  source: z.string().min(1),  excerpt: z.string().min(1)});export const preparedClaimSchema = z.object({  transcript: z.string().min(1),  product: productSchema,  category: z.string().min(1),  extractedFields: z.record(z.string(), z.string()),  summary: z.string().min(1),  procedure: procedureCitationSchema,  responsibleArea: z.string().min(1),  missingInformation: z.array(z.string()),  draftResponse: z.string().min(1),  confidence: z.number().min(0).max(1),  timingsMs: z.record(z.string(), z.number().nonnegative()),  inference: z.object({    provider: z.literal("QVAC"),    location: z.literal("local"),    models: z.array(z.string())  })});export type Product = z.infer<typeof productSchema>;export type ClaimStage = z.infer<typeof claimStageSchema>;export type IntakeInput = z.infer<typeof intakeInputSchema>;export type PreparedClaim = z.infer<typeof preparedClaimSchema>;export interface ClaimProgress {  stage: ClaimStage;  elapsedMs: number;}export interface ConfirmedClaim extends PreparedClaim {  id: string;  confirmedAt: string;}export interface ClaimRunSnapshot {  id: string;  status: ClaimStage;  elapsedMs: number;  transcript?: string;  result?: PreparedClaim;  error?: { code: string; message: string };}